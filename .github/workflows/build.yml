# This workflow is disabled
# Builds only run when tags are pushed (see build-release.yml)
# PR builds are handled by build-pr.yml

name: Build (Disabled)

on:
  workflow_dispatch:  # Only available for manual runs if needed
    inputs:
      force_build:
        description: 'Force build (for testing)'
        required: false
        default: 'false'

jobs:
  disabled:
    name: Workflow Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "This workflow is disabled."
          echo "Builds only run on tag push (see build-release.yml)."
          echo "For PR builds, see build-pr.yml."
